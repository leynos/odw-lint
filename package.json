{
  "name": "odw-lint",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "ISC",
  "scripts": {
    "build": "tsc",
    "fmt": "bunx biome format --write src tests package.json biome.jsonc bunfig.toml tsconfig.json",
    "lint": "bunx biome ci src tests",
    "check:types": "tsc --noEmit",
    "test": "bun test",
    "test:all": "bun run lint && bun run check:types && bun test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.1",
    "bun-types": "^1.1.3",
    "typescript": "^5.6.3"
  }
}
